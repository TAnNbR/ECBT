{
  "name": "orderbook-subgraph",
  "version": "1.0.0",
  "description": "OrderBook Subgraph",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 orderbook",
    "create-local": "graph create --node http://localhost:8020/ orderbook",
    "remove-local": "graph remove --node http://localhost:8020/ orderbook"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.35.1"
  }
}

